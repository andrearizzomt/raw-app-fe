import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Static HTML export creating /out folder used to deploy the landing page on Vercel
  // output: "export",
};

export default nextConfig;
